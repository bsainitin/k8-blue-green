apiVersion: v1 
kind: Service
metadata:
  name: main-service
spec:
  selector:
    purpose: blue-green
    project: roboshop
    colour: blue
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80